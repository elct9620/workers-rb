# How many Hosts answer. Each one is identical — no setting below belongs to
# one of them rather than another. One Host answers requests; several are what
# make `Env.node` worth reading.
#
# Each Host keeps up to 5 statements in flight and one server holds 128
# connections open, so raising this far enough has Hosts waiting on the server
# rather than on their own pool. README sizes the two together.
nodes: 3

# The suffix under which `<tenant>.<base>` reaches a Tenant. A Tenant also
# answers at `/<tenant>` and at whatever domain its Manifest declares, neither
# of which this names.
baseDomain: workers.test

# How long a request still running may hold up a Host that was told to stop.
#
# Left to itself the Host waits for it forever, which is right wherever a
# request cannot outlive its own work. Here it can: the shared directory is a
# mount, and one that stops answering leaves the request in a call no signal
# reaches — so a Host that cannot finish stopping holds up the rolling update
# it was stopped for, or the machine's own shutdown.
#
# Zero drops what is still running rather than waiting for it. Waiting without
# end is what this chart does not offer, because a Kubernetes that has already
# decided to stop this Host ends the wait with a kill either way.
shutdownTimeoutSeconds: 10

# What one Host costs the machine it runs on.
#
# The floor is the process and the compiled guest module, and a Host pays it
# once however many Tenants it has cached: a Sandbox holds no interpreter
# between invocations, so what grows under load is the number of requests in
# flight, never the number of Tenants. The ceiling is that floor plus every
# request running tenant code at its memory cap at once — five and 16 MiB
# today, both of them the Host's settings rather than this chart's, so raising
# either raises what belongs here.
#
# Memory has a limit because a Host past it takes the machine's other tenants
# with it. CPU has none: a Host held back serves slowly, and one killed for
# being busy serves nothing.
resources:
  requests:
    cpu: 100m
    memory: 160Mi
  limits:
    memory: 320Mi

image:
  repository: ghcr.io/elct9620/workers-rb/host
  # Empty is the Host this chart was released with, which is what pinning the
  # chart's version already pinned. Naming a tag here runs a different Host
  # than the release names, and is what makes `helm upgrade` roll the Pods —
  # a tag that has not changed leaves them where they are.
  tag: ""
  pullPolicy: IfNotPresent

# Where Tenants are published. Every Host reads the same one, so a change made
# through it reaches the next request on all of them.
#
# The claim is the operator's: what backs it decides how Tenants get written
# into it, and how many Nodes can mount it for reading is that same decision.
# There is no default, because a directory only one Host can read makes a
# cluster that disagrees with itself about which Tenants exist, and it does so
# silently.
sharedDirectory:
  existingClaim: ""

databases:
  # False leaves the databases to a server this chart did not install, named by
  # `url` and `adminUrl` below.
  deploy: true
  url: ""
  adminUrl: ""

  image: ghcr.io/tursodatabase/libsql-server:v0.24.32

  # What the server costs. It answers statements against files it holds open,
  # so what it needs follows how much is in flight and how much write-ahead log
  # stands between checkpoints — not how many Tenants have declared a database.
  # A limit for the same reason a Host has one, and no CPU limit for the same
  # reason a Host has none.
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      memory: 256Mi

  # One server holds every Tenant's database, so this is where they all live.
  # Empty follows the cluster's default class — which decides whether a lost
  # machine means the database is rescheduled or waited for.
  #
  # NFS does not serve here: SQLite's locking is what a shared directory can
  # afford to be casual about and a database cannot.
  storageClass: ""
  size: 10Gi

  # sqld finishes a checkpoint only when no reader holds the write-ahead log,
  # so this is how soon a quiet moment is used rather than how often one is
  # forced. README says how to choose it for traffic that is not the demo's.
  checkpointIntervalSeconds: 30
